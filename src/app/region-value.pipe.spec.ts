import { RegionValuePipe } from './region-value.pipe';

describe('RegionValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RegionValuePipe();
    expect(pipe).toBeTruthy();
  });
});
